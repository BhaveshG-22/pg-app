{
  "name": "pixelglow-worker",
  "version": "1.0.0",
  "description": "Background worker for PixelGlow image processing",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "worker": "node index.js",
    "postinstall": "prisma generate --schema=./schema.prisma",
    "prisma:generate": "prisma generate --schema=./schema.prisma"
  },
  "dependencies": {
    "bull": "^4.12.2",
    "ioredis": "^5.3.2",
    "prisma": "^5.7.1",
    "@prisma/client": "^5.7.1",
    "aws-sdk": "^2.1691.0",
    "dotenv": "^16.3.1",
    "sharp": "^0.33.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}